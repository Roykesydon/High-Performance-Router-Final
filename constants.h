#ifndef CONSTANTS_H
#define CONSTANTS_H
#define ll long long

const unsigned int RULE_COUNT = 953;

// constraints
const ll INITIAL_RANGE = 20;
const ll DELTA = 3;
const ll MAX_WAITING_PACKETS = 10;
const ll MAX_WAITING_FLOWS = 60;

const ll TABLE_ONE_THRESHOLD = 500;

#endif  // CONSTANTS_H